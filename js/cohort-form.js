/**
 * BWAI — Future cohort registration scaffold
 *
 * This file intentionally does not connect to Firebase yet.
 * When cohort registration opens:
 * - collect only the information you actually need;
 * - validate the form on the client and server/rules where applicable;
 * - write registrations to a Firestore collection such as `cohortLeads`;
 * - add Firebase Security Rules before accepting real submissions.
 *
 * Suggested fields:
 *   name, email, phone (optional), experienceLevel, createdAt
 *
 * Suggested collection:
 *   cohortLeads/{autoId}
 */

// Future module imports:
// import { initializeApp } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js";
// import { getFirestore, collection, addDoc, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js";
// import { firebaseConfig } from "./firebase-config.js";

// Future initialization:
// const app = initializeApp(firebaseConfig);
// const db = getFirestore(app);

// Keep the current "Coming Soon" modal until registration is officially opened.
